<!-- template: slug=onboarding profile=guide — see _templates/README.md for the markup contract -->

# <Title — "Onboarding — <one-line scope>">

<!-- fill: badge row (spec/source/index), left as the pipeline stamps them -->

> **Summary.** <one sentence; mirror the manifest summary>

## Overview

<!-- fill: 1 short paragraph. What onboarding produces (a valid sevn.json), that every
     setup path shares one pipeline, and that re-running is safe. Operator terms. -->

## First-time setup

<!-- fill: the ordered first-run steps (make setup → sevn onboard → sevn doctor), then
     the subsections below. Expected:
       ### Web wizard vs CLI          — surface/command/when table
       ### Telegram during onboarding — bot token + user API credential step
       ### Profiles (`src/sevn/data/onboarding_profiles/`) — what a profile selects
       ### Daemon install             — launchd/systemd units -->

## Re-running safely

<!-- fill: reuse-vs-wipe behaviour, idempotency, what promote does -->

## Daily operations

<!-- fill: the handful of commands an operator runs after setup (start/stop/status) -->

## References
