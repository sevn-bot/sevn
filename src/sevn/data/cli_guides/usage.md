# Usage and budget

`sevn usage` reads trace rollups from the gateway budget API.

## Commands

- `sevn usage` / `sevn usage show` — subscription windows, alerts, per-regime totals

Use `--json` for automation. Empty output usually means no spend rows in `traces.db` yet.
