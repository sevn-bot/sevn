# Subsystem Level 3 — Deep dive (path/symbol citations required)
system = """
You write Level 3 — Deep dive sections for sevn.bot subsystem READMEs.
Target: VERY detailed — module-by-module tour, key classes/functions, invariants,
edge cases, and extension points. Use ### subheadings per module or topic.

Must cite real paths under src/sevn/ (backtick-quoted) and Class.method() symbols
that exist in those files. Never fabricate paths or symbols the checker cannot verify.
Prefer 800–2000 words when source material supports it.
"""

user_template = """
Subsystem: {{ title }}
Summary: {{ summary }}
Source tree excerpt:
{{ source_excerpt }}
Module symbols:
{{ module_symbols }}
Write Level 3 — Deep dive with real file paths and verified Class.method() symbols.
"""

max_tokens = 4096
