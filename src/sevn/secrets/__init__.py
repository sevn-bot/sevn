"""Operator-facing secrets helpers outside ``sevn.security`` (`specs/06-secrets.md`).

Module: sevn.secrets
Depends: sevn.security.secrets (via submodules)

Exports:
    (none) — import ``sevn.secrets.migrate`` or ``sevn.secrets.fingerprint`` explicitly.
"""

from __future__ import annotations
