"""Workspace memory helpers (LCM-adjacent; optional subsystems).

Module: sevn.memory
Depends: (none at package root)

Exports:
    (none — import ``sevn.memory.user_model`` explicitly.)

Examples:
    >>> import sevn.memory as m
    >>> hasattr(m, "__doc__")
    True
"""

from __future__ import annotations
