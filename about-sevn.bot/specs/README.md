# Spec index

Generated by `sevn about-docs index`. Do not edit by hand.

| id | title | status | parent_prd | summary |
| --- | --- | --- | --- | --- |
| spec-00-foundation | Foundation — Spec | scaffold | prd-00-main | Deliver the lowest layer every later spec assumes: a src/sevn/ package layout, uv-managed Python 3.12+ project (hatchling build backend), a root Makefile as the single recurring-command surface, pre-c |
| spec-01-system-overview | System overview — Spec | scaffold | prd-00-main | Give implementers a single picture of the runtime before feature work: package boundaries under src/sevn/, allowed import directions, and the shared protocols that keep LLM wiring, observability, and |
| spec-02-config-and-workspace | Config and workspace — Spec | scaffold | prd-06-setup-and-operations | Provide a single, testable configuration surface before storage, tracing, proxy, and gateway work: locate sevn.json, validate schema_version and structured subtrees needed by early boot, resolve the c |
| spec-03-storage | Storage — Spec | scaffold | prd-02-personality-and-memory | Own application persistence: connection setup (WAL, foreign keys), versioned migrations, canonical sevn.db path, optional traces.db path helper, and typed persistence contracts for crash-resume and (w |
| spec-04-tracing | Tracing — Spec | scaffold | prd-07-mission-control | Provide durable trace sinks that implement TraceSink without ever throwing through emit, so instrumentation stays off the critical path. SQLite layout matches Mission Control query patterns (prd-07-mi |
| spec-05-llm-transports | LLM transports — Spec | scaffold | prd-05-cost-and-providers | Normalize provider-shaped JSON over async HTTP to a single egress base URL (SEVN_PROXY_URL / ProcessSettings.proxy_url), so tier executors bind once per turn and never touch raw secrets. LiteLLM may r |
| spec-06-secrets | Secrets — Spec | scaffold | prd-03-trust-and-control | Deliver a single trust boundary for credentials: backend modules + TTL cache under src/sevn/security/, wired exclusively by the egress proxy (src/sevn/proxy/) so agent-facing processes never see raw k |
| spec-07-egress-proxy | Egress proxy — Spec | scaffold | prd-03-trust-and-control | Product pairing (v1). Deployment, paired daemon install, onboarding validation, and Mission Control management of the proxy are specified in prd-06-setup-and-operations and prd-07-mission-control §5.1 |
| spec-08-sandbox | Sandbox — Spec | scaffold | prd-03-trust-and-control | Deliver a single tool-execution sandbox used by sandbox_exec, exec / safebash (when routed through the execution sandbox), process when configured for sandbox routing, and skill subprocesses spawned b |
| spec-09-security-scanner | Security scanner — Spec | scaffold | prd-03-trust-and-control | Deliver a single scanner subsystem that runs in the gateway process so hostile content is filtered before the Triager or any routing model sees user-visible text, transcripts, or selected tool output. |
| spec-10-schema-ontology | Schema & ontology — Spec | scaffold | prd-04-getting-things-done | Define the runtime ontology for Triager output and related labels across the agent core: canonical field names, closed enums, typing conventions, and how they compose with executor dispatch described |
| spec-11-tools-registry | Tools registry — Spec | scaffold | prd-04-getting-things-done | Own the Layer-3 tool callables and Layer-2 framework adapters that every executor tier uses: one implementation per tool name, registered in a session-scoped ToolSet, exposed to LLM frameworks without |
| spec-12-skills-system | Skills system — Spec | scaffold | prd-04-getting-things-done | Own everything under workspace/skills/: how skills are discovered, validated, indexed for routing (spec-10-schema-ontology TriageResult.skills holds names only — descriptions come from this subsystem) |
| spec-13-rlm-triager | RLM Triager — Spec | scaffold | prd-04-getting-things-done | The Triager is the routing brain (prd-04-getting-things-done §5.1–§5.2): a single, tool-less outbound generation step that emits validated TriageResult consumed by tier dispatch (A / B / C / D), MCP e |
| spec-14-executor-tier-b | Executor tier B — Spec | scaffold | prd-04-getting-things-done | Tier B is the default “do work” executor for messages the Triager classifies as complexity == B (prd-04-getting-things-done §5.2): a single pydantic-ai Agent loop over the user’s incoming_text, with t |
| spec-15-memory-lcm | Memory — LCM (Lossless Context Management) — Spec | scaffold | prd-02-personality-and-memory | LCM is the lossless conversation memory for a workspace (prd-02-personality-and-memory §5.2–§5.4): every qualifying message is stored; compaction summarises without deleting source rows; the assembler |
| spec-16-harness-discipline | Harness discipline — Spec | scaffold | prd-04-getting-things-done | Harness discipline: background task logging, operator PATH augmentation, and gateway/agent harness hooks under agent/harness/. |
| spec-17-gateway | Gateway — Spec | scaffold | prd-01-conversational-experience | Run the long-lived gateway process that accepts channel ingress (Telegram poll/webhook, webchat WS), normalises messages, enforces trust boundaries (scanner, rate limits), persists session history, an |
| spec-18-channel-telegram | Channel — Telegram — Spec | scaffold | prd-01-conversational-experience | Deliver the primary daily-driver channel for personal messaging: a ChannelAdapter implementation that normalises Telegram Updates into spec-17-gateway IncomingMessage / OutgoingMessage and implements |
| spec-19-channel-webui | Channel — Web UI — Spec | scaffold | prd-01-conversational-experience | Deliver the browser conversational surface required by prd-01-conversational-experience §5.1: owner-only WebSocket chat, static SPA shell, and session continuity with Telegram via the shared SessionMa |
| spec-20-voice | Voice (STT / TTS) — Spec | scaffold | prd-01-conversational-experience | Own the provider-chain facades for speech-to-text and text-to-speech so the gateway can: |
| spec-21-executor-tier-cd | Executor tier C/D — Spec | scaffold | prd-04-getting-things-done | Tier C/D is the planned-work executor for messages the Triager classifies as complexity == C or complexity == D (prd-04-getting-things-done §5.3–§5.4): structured planning, optional owner approval (Pl |
| spec-22-onboarding | Onboarding — Spec | scaffold | prd-06-setup-and-operations | Deliver the merge + validation + promotion pipeline every setup path shares so sevn.json stays the single source of truth (prd-06-setup-and-operations §5.4, spec-02-config-and-workspace): shipped pres |
| spec-23-cli | CLI — Spec | scaffold | prd-06-setup-and-operations | Deliver the primary operator and automation surface for install, upgrades, health checks, workspace + daemon lifecycle, and scriptable inspection. The CLI is not the agent’s in-harness tool API and no |
| spec-24-dashboard | Mission Control (dashboard) — Spec | scaffold | prd-07-mission-control | Deliver Mission Control: a same-process dashboard (prd-07-mission-control) so the owner can inspect traces, costs, provider health, in-flight runs, proxy status, and config without opening SQLite from |
| spec-25-cicd-full | CI/CD (mature pipeline) — Spec | scaffold | prd-06-setup-and-operations | Grow spec-00-foundation’s minimal verify loop into a phase-strict delivery pipeline: broader CI matrices, checked-in Dockerfile validation for spec-08-sandbox (and any ASGI image built for spec-07-egr |
| spec-26-claude-agent | Claude Agent — Spec | rejected | prd-08-coding-companion | - N/A: Spec rejected — no implementation rows for v0.0.2. |
| spec-27-second-brain | Second Brain — Spec | scaffold | prd-09-knowledge-base | Deliver the Second Brain subsystem: filesystem wiki engine + agent surface so operators curate sources in raw/ and maintain a structured wiki/ with index.md, log.md, lint reports, and provenance-beari |
| spec-28-code-understanding | Code understanding — Spec | scaffold | prd-08-coding-companion | Deliver the code-orientation stack the coding companion PRD names: five orthogonal capabilities (MYCODE, Memgraph CGR, code-review-graph (SQLite MCP), roam-code, Graphify) that Triager and executors c |
| spec-29-cursor-cloud-agent | Cursor Cloud Agent — Spec | scaffold | prd-04-getting-things-done | Let operators and agents launch, poll, and inspect Cursor Cloud Agents against any GitHub/GitLab repo when skills.cursor_cloud.enabled is true, returning PR URLs, dashboard links (remote desktop), and |
| spec-30-non-interactive-triggers | Non-interactive triggers — Spec | scaffold | prd-11-automation-and-triggers | Deliver non-interactive dispatch: external events (“something happened”) and schedules (“tick”) compile to DispatchRequest, optionally pass through notify_only (zero LLM, zero sandbox boot), otherwise |
| spec-31-memory-dreaming | Memory — Dreaming — Spec | scaffold | prd-02-personality-and-memory | Provide scored consolidation from short-term recall signals into curated long-term prose (MEMORY.md) on a daily (configurable) cadence, without mutating LCM tables or crossing into Second Brain (wiki/ |
| spec-32-memory-honcho | Memory — Honcho-style user model — Spec | scaffold | prd-02-personality-and-memory | Deliver an opt-in inferred profile that accumulates stable operator-facing facts (preferences, recurring context the operator states in chat) without requiring manual USER.md edits for every drift. Wh |
| spec-33-self-improvement | Self-improvement — Spec | scaffold | prd-12-self-improvement | Deliver src/sevn/self_improve/: ingest traces + session artefacts + explicit user feedback into trajectory_fact rows, deterministically shortlist turns for review or patching, optionally run an in-pro |
| spec-34-plugin-hooks | Plugin hooks & channel plugins — Spec | scaffold | prd-13-extensibility | Deliver the in-process extension layer that intercepts existing tool and terminal I/O paths and registers dispatcher-level commands, without adding new tool symbols or transports in-tree. |
| spec-35-bot-evolution | Bot evolution — Spec | scaffold | prd-07-mission-control | Deliver src/sevn/evolution/ and the operator-facing Evolution surface so sevn.bot can evolve its own codebase as a first-class product pillar — not an optional add-on — spanning understand → file work |
| spec-36-sub-agents | Sub-agents (L1/L2) — Spec | done | prd-04-getting-things-done | Level-1 sub-agents (tracked, concurrent, killable role runs) that may spawn level-2 workers (incl. specialists); multi queue mode; limits, tracing, kill surfaces, media_generation skill. |
| spec-37-openui | OpenUI — Spec | scaffold | prd-10-generated-ui | Deliver OpenUI: explicit openui_render tool calls produce sanitised, CSP-wrapped, size-capped HTML (live or rasterised) and deterministic form callbacks that rejoin the same executor turn for tier B / |
