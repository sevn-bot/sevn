# Golden fixtures — `user_model` (`specs/32-memory-honcho.md` §10.4)

`profile_golden.json` is a minimal **`UserProfile`** snapshot used by integration-style tests (merge + render paths) without calling a live LLM.
