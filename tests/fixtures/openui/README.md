# OpenUI test fixtures

Golden HTML snippets for sanitiser / integration tests (`specs/29-openui.md` §9).

Add small `.html` fragments here when a regression needs a stable on-disk corpus; keep files ASCII and free of secrets.
