Agent Tools & Permissions Live tool approvalsNo pending tool executions — dangerous tools pause here when a turn needs operator approval. Shared ToolSkillHealthService — same data as Telegram Tools & Skills → Health. Tool-layer chronic rows ship when the tools health table is wired. No rows yet. 0 row(s) · threshold ≥ 3 in 14 d Permissions & tools config Edits permissions and tools in sevn.json (JSON objects). permissions { "mc-e2e-w3b-probe": true } tools {} Save permissions & tools
